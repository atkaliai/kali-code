# Agent Instructions for this Repository

- Do **not** change the configured default model identifier in `interactive.py` unless the user explicitly asks for that exact change in the current request.
- Preserve existing API/base URL wiring when modifying model behavior.
- Keep user-facing branding/messages stable unless explicitly requested.
